# Print Method for Bootstrap Validation Results

Formats and displays an operational runtime summary of a compiled
`validate_cutpoint_result` object canvas.

## Usage

``` r
# S3 method for class 'validate_cutpoint_result'
print(x, ...)
```

## Arguments

- x:

  A `validate_cutpoint_result` object.

- ...:

  Additional arguments passed down to downstream rendering pipelines.

## Value

The input object invisibly.

## srrstats compliance

.
