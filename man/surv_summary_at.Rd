% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-surv-summary.R
\name{surv_summary_at}
\alias{surv_summary_at}
\title{Get a Tidy Summary of Survival Probabilities at Specific Time Points}
\usage{
surv_summary_at(fit, times, data_time_unit, summary_time_unit = "years")
}
\arguments{
\item{fit}{An object of class `survfit`, typically created by `survival::survfit()`.}

\item{times}{A numeric vector of time points at which to get the survival summary.}

\item{data_time_unit}{The unit of the 'time' variable in the original dataset
used to create the `fit` object. Must be one of "hours", "days", "months", or "years".}

\item{summary_time_unit}{The unit of the `times` argument. Defaults to "years".
Must be one of "hours", "days", "months", or "years".}
}
\value{
A tidy data frame summarizing the survival probability, confidence
  intervals, and number of subjects at risk for each stratum at each
  specified time point.
}
\description{
A user-friendly wrapper around `summary.survfit` that automatically handles
time unit conversions to provide survival estimates at desired time points
(e.g., 1, 3, and 5 years), regardless of the original data's time unit.
}
\examples{
\dontrun{
library(survival)

# Load example data where time is in days
data(lung)
# Recode status to be 0/1 for this example
lung$status <- ifelse(lung$status == 2, 1, 0)

# Create a survfit object
km_fit <- survfit(Surv(time, status) ~ sex, data = lung)

# Get the 1-year and 2-year survival probabilities
# The function will automatically convert years to days for the summary.
surv_summary_at(
  fit = km_fit,
  times = c(1, 2),
  data_time_unit = "days",
  summary_time_unit = "years"
)
}
}
