% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_cutpoint.R, R/find_cutpoint_methods.R
\name{find_cutpoint}
\alias{find_cutpoint}
\alias{print.find_cutpoint}
\alias{summary.find_cutpoint}
\title{Find Optimal Cut-points for Survival Data}
\usage{
find_cutpoint(
  data,
  predictor,
  outcome_time,
  outcome_event,
  num_cuts = 1,
  method = c("systematic", "genetic"),
  criterion = c("logrank", "hazard_ratio", "p_value"),
  covariates = NULL,
  nmin = 20,
  seed = NULL,
  max.generations = NULL,
  pop.size = NULL,
  n_perm = 0,
  n_cores = 1,
  use_cpp = TRUE,
  grid_by = 0.01,
  quiet = FALSE,
  candidate_cuts = NULL,
  ...
)

\method{print}{find_cutpoint}(x, ...)

\method{summary}{find_cutpoint}(
  object,
  show_model = TRUE,
  show_group_counts = TRUE,
  show_medians = TRUE,
  show_ph_test = TRUE,
  show_params = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame containing the analysis variables.}

\item{predictor}{The continuous predictor variable name (character).}

\item{outcome_time}{The time-to-event variable name (character).}

\item{outcome_event}{The event status variable name (character, 0 or 1).}

\item{num_cuts}{The number of cut-points to find. Default is 1.}

\item{method}{Algorithm search type: `"systematic"` or `"genetic"`.}

\item{criterion}{The statistic to optimise: `"logrank"`, `"hazard_ratio"`, or `"p_value"`.}

\item{covariates}{Character vector of covariate names (optional).}

\item{nmin}{Min. group size (integer count or proportion).}

\item{seed}{Optional integer seed for reproducible genetic search.}

\item{max.generations}{Max generations for genetic algorithm. If `NULL`, dynamically scales.}

\item{pop.size}{Population size for genetic algorithm. If `NULL`, dynamically scales.}

\item{n_perm}{Number of permutations to run for an adjusted p-value. Default is 0.}

\item{n_cores}{Number of CPU cores for parallel permutations. Default is 1.}

\item{use_cpp}{Logical. Checks and calls compiled C++ routines via `Rcpp`. Default is `TRUE`.}

\item{grid_by}{Percentile step increment for systematic grid downsampling. Default is 0.01.}

\item{quiet}{Logical. If `TRUE`, suppresses operational console alerts.}

\item{candidate_cuts}{Optional vector of pre-filtered cuts defining a narrow search space.}

\item{...}{Additional arguments passed down to downstream rendering pipelines.}

\item{x}{A find_cutpoint result object.}

\item{object}{A find_cutpoint result object for summary evaluation.}

\item{show_model}{Logical. Whether to print the full Cox model summary frame.}

\item{show_group_counts}{Logical. Whether to show stratified sample split counts.}

\item{show_medians}{Logical. Whether to display Kaplan-Meier median tracking times.}

\item{show_ph_test}{Logical. Display the proportional hazards validation check.}

\item{show_params}{Logical. Print original baseline parameters.}
}
\value{
An object of class `find_cutpoint` containing the
     optimal cut-points, statistic, and analysis parameters.
}
\description{
Finds optimal cut-point(s) for a continuous predictor in a
time-to-event (survival) analysis. Uses systematic search (1–2
cuts) or a genetic algorithm (any number of cuts). Features high-speed
integer partitioning via compiled C++ vector assignments and automated
quantile grid downsampling.
}
\details{
`method = "systematic"`: grid search respecting `nmin`. Optimised via internal quantiles.
`method = "genetic"`: `rgenoud` global optimisation.
Systematic search is slow for `num_cuts > 2`; use `genetic`.
Core vector partitions are calculated in compiled C++ via `Rcpp` for optimal performance.
}
\section{srrstats compliance}{

.


.


.
}

\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  library(survival)

  # Create a lightweight, reproducible simulation baseline dataset
  set.seed(42)
  sim_data <- data.frame(
    time = rexp(30, rate = 0.1),
    event = sample(c(0, 1), 30, replace = TRUE),
    biomarker = rnorm(30, mean = 5, sd = 1.5)
  )

  # Execute an exhaustive systematic threshold discovery sweep
  fit <- find_cutpoint(
    data = sim_data,
    predictor = "biomarker",
    outcome_time = "time",
    outcome_event = "event",
    num_cuts = 1,
    method = "systematic",
    criterion = "logrank",
    nmin = 5,
    quiet = TRUE
  )
  print(fit)
}
}
\references{
Altman, D. G., Lausen, B., Sauerbrei, W., & Schumacher,
M. (1994). Dangers of Using “Optimal” Cutpoints in the Evaluation of
Prognostic Factors. *JNCI: Journal of the National Cancer Institute*,
86(11), 829–835. \doi{10.1093/jnci/86.11.829}

Cox, D. R. (1972). Regression Models and Life-Tables. *Journal
of the Royal Statistical Society: Series B (Methodological)*, 34(2),
187–202. \doi{10.1111/j.2517-6161.1972.tb00899.x}

Mantel, N. (1966). Evaluation of survival data and two new
rank order statistics arising in its consideration. *Cancer
Chemotherapy Reports*, 50(3).

Mebane Jr, W. R., & Sekhon, J. S. (2011). Genetic
Optimization Using Derivatives: The rgenoud Package for R.
*Journal of Statistical Software*, 42, 1–26.
\doi{10.18637/jss.v042.i11}
}
