% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R
\name{plot_cutpoint_residuals}
\alias{plot_cutpoint_residuals}
\title{Diagnostic Plot of Schoenfeld Residuals}
\usage{
plot_cutpoint_residuals(x, ...)
}
\arguments{
\item{x}{A \code{find_cutpoint} result object.}

\item{...}{Unused optional arguments.}
}
\value{
A publication-ready \code{ggplot} canvas frame, or \code{NULL} if the fit fails.
}
\description{
High-tier multi-panel diagnostic dashboard tracking the proportional hazards
assumption with custom facets per risk cohort stratum.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  library(survival)
  mock_df <- data.frame(time = 1:30, event = rep(c(0, 1), 15), factor = rnorm(30))
  res <- find_cutpoint(
    mock_df, "factor", "time", "event",
    num_cuts = 1, method = "systematic", quiet = TRUE
  )
  p <- plot_cutpoint_residuals(res)
}
}
