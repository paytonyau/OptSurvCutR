% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R,
%   R/validate_cutpoint_methods.R
\name{plot_validation}
\alias{plot_validation}
\alias{print.validate_cutpoint_result}
\alias{plot.validate_cutpoint_result}
\alias{summary.validate_cutpoint_result}
\title{Plot Cut-point Optimisation Stability Surface and Help Catalog Page}
\usage{
plot_validation(
  validation_result,
  main = "Resampling Convergence & Stability Landscape",
  focus_cuts = c(1, 2),
  ...
)

\method{print}{validate_cutpoint_result}(x, ...)

\method{plot}{validate_cutpoint_result}(x, ...)

\method{summary}{validate_cutpoint_result}(
  object,
  show_descriptives = TRUE,
  show_ci = TRUE,
  show_params = TRUE,
  plot.it = FALSE,
  ...
)
}
\arguments{
\item{validation_result}{A validation object generated by OptSurvCutR containing
the validation log history dataset.}

\item{main}{Main title of the chart canvas. Defaults to "Resampling Convergence & Stability Landscape".}

\item{focus_cuts}{A numeric vector of length 2 specifying which two cut-points to map
if the model contains more than 2 cuts. Defaults to \code{c(1, 2)}.}

\item{...}{Additional arguments passed down to downstream rendering pipelines.}

\item{x}{A validation result object for generic printing or plotting methods dispatch.}

\item{object}{A validation result object passed to down-stream summary tracking.}

\item{show_descriptives}{Logical. Show complete bootstrap distribution descriptives? Defaults to \code{TRUE}.}

\item{show_ci}{Logical. Print 95\% Empirical Confidence Interval boundaries? Defaults to \code{TRUE}.}

\item{show_params}{Logical. Display original metadata loop execution parameters? Defaults to \code{TRUE}.}

\item{plot.it}{Logical. If \code{TRUE}, automatically outputs the visual sampling line charts. Defaults to \code{FALSE}.}
}
\value{
A publication-ready \code{ggplot} canvas object displaying stability density bounds.
}
\description{
Generates a premium, continuous 2D contour surface density topology map tracking the
statistical stability of paired discovered cut-points across bootstrap resampling
profiles. Automatically handles 1, 2, or multi-cut architectures dynamically.
}
\section{srrstats compliance}{

.


.


.
}

\examples{
mock_val <- list(
  bootstrap_distribution = data.frame(Cut_point_1 = rnorm(30, 10, 1)),
  original_cuts = 10.2,
  parameters = list(predictor = "Biomarker", num_replicates = 30, successful_reps = 30)
)
p <- plot_validation(mock_val)
}
