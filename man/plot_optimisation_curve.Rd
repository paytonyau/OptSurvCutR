% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R
\name{plot_optimisation_curve}
\alias{plot_optimisation_curve}
\title{Plot Optimisation Curve or Surface from Search}
\usage{
plot_optimisation_curve(cutpoint_result, ...)
}
\arguments{
\item{cutpoint_result}{A \code{find_cutpoint} object generated with \code{method = "systematic"}.}

\item{...}{Unused dots.}
}
\value{
A valid \code{ggplot} object detailing evaluation statistics vs coordinates.
}
\description{
Plots the metric landscape evaluated across coordinates. Maps a 1D optimisation line
for 1-cut systematic setups, or a 2D topographic grid profile for 2-cut layouts.
}
\section{srrstats compliance}{

.
}

\examples{
library(survival)
data(pbc, package = "survival")
pbc_sub <- na.omit(pbc[1:60, c("time", "status", "bili")])
pbc_sub$event <- as.integer(pbc_sub$status \%in\% c(1, 2))

# Execute a minimal 1-cut systematic search to generate the grid landscape
res <- find_cutpoint(
  data          = pbc_sub,
  predictor     = "bili",
  outcome_time  = "time",
  outcome_event = "event",
  num_cuts      = 1,
  method        = "systematic"
)

# Plot the 1D metric optimisation curve
plot_optimisation_curve(res)

}
